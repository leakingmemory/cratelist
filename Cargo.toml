[package]
name = "cratelist"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
toml = "0.7.8"
clap = { version = "4.6.0", features = ["derive"] }
